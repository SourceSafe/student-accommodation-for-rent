import { Outlet } from "react-router-dom";
const Layout = () => {
  return (
    <div>
      <>Student Accommodation For Rent - (Logo)</>                 
      <Outlet />
    </div>
  )
};

export default Layout;